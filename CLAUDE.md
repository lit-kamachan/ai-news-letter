# ai-news-letter — 運用ルール

毎朝07:03(JST)に無人実行される自動ニュースレターです。GitHub Pages は `main` ブランチのルートから配信されます（`https://lit-kamachan.github.io/ai-news-letter/`）。`seen.json` の `issues`/`urls` が既出判定と号数採番の唯一の正データです。

## 必ず守ること（2026-08-17の事故を踏まえて）

過去に、セッションごとに割り当てられた作業ブランチ（例: `main-milwtc`）が `main` から取り残された状態で作業を始めてしまい、(1) 別セッションが別ブランチ（`main-syw45h`）で作った号がマージされずに残る、(2) 両方のセッションが `main` の古い `seen.json` しか見ておらず同じ号数を採番する、という事故が起きた（第3号が2つできた）。再発防止のため、**どのブランチを割り当てられていても**以下を必ず実行する。

1. **作業開始時に `git fetch origin main` を実行し、`origin/main` の最新コミットを正とする。** 割り当てられた作業ブランチ（`main-milwtc` 等）がこれより古い場合は使わず、`origin/main` から新しいブランチを切り直す（例: `git checkout -b <work-branch> origin/main`）。
2. **作業開始時に `main` 宛の open PR がないか確認する**（`list_pull_requests` state=open, base=main）。前回セッションの未マージ号が残っていたら、内容を確認し先にマージ（または破棄をユーザーに確認）してから新しい号の採番・収集を始める。号数・`seen.json` はマージ後の状態を基準に決める。
3. **作業完了時は `main` へ確実に反映する。** 直接pushできればpush、できなければPRを作成してその場でマージする（ユーザーの承認が必要な場合を除く）。**作業用ブランチに置いたまま session を終えない。**
4. マージ後、使い終わった作業用ブランチは残しても構わないが、次回セッションは必ず1.に従って `origin/main` を基準に取り直すこと（古いブランチをそのまま使い続けない）。

## ファイル構成

- `news/YYYY-MM-DD.html` — 号ごとのニュースレター本体
- `index.html` — バックナンバー一覧（新しい号が先頭・既存行は削除しない）
- `seen.json` — `issues`（号ごとのメタ情報）と `urls`（既出URL正規化リスト）
